@page
@model ASP.Areas.Admin.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>     <a asp-page="Create" class="btn btn-outline-info"> 
    <span class="fa fa-plus-circle"> Add</span></a> </p><table class="table">
    <thead>
    <tr>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.Boots[0].BootsName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Boots[0].Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Boots[0].Size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Boots[0].Image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Boots[0].Group)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Boots) {
    <tr>
        <td>
             <img src="~/Images/@item.Image" alt="NoImage" width="50"/>
        </td> 
        <td>
            @Html.DisplayFor(modelItem => item.BootsName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Size)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Image)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Group.GroupName)
        </td>
        <td>
            
            <a asp-page="./Edit" asp-route-id="@item.BootsId"
               class="btn btn-outline-info">
                <span class="fa fa-pencil"></span>
            </a>
            <a asp-page="./Delete" asp-route-id="@item.BootsId"
               class="btn btn-outline-danger"> 
                <span class="fa fa-close"></span></a>   

            @*<a asp-page="./Edit" asp-route-id="@item.BootsId">Edit</a> |
            <a asp-page="./Details" asp-route-id="@item.BootsId">Details</a> |
            <a asp-page="./Delete" asp-route-id="@item.BootsId">Delete</a>*@
        </td>
    </tr>
}
    </tbody>
</table>
